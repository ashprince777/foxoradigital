version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: agencyflow_postgres
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-agencyflow}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: agencyflow_redis
    ports:
      - "6379:6379"

volumes:
  postgres_data:
